{
  "name": "mocha-ui-exports-auto-describe",
  "version": "1.0.6",
  "description": "Like the builtin exports UI but also adds a describe block per-file.",
  "repository": "dword-design/mocha-ui-exports-auto-describe",
  "license": "MIT",
  "author": "Sebastian Landwehr <info@dword-design.de>",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "base clean",
    "commit": "base commit",
    "dev": "base dev",
    "lint": "base lint",
    "prepare": "base prepare",
    "prepublishOnly": "base prepublishOnly",
    "test": "base test"
  },
  "dependencies": {
    "@dword-design/functions": "^2.7.2",
    "mocha": "^8.0.0"
  },
  "devDependencies": {
    "@dword-design/base": "^6.0.13",
    "execa": "^4.0.0",
    "output-files": "^1.1.15",
    "with-local-tmp-dir": "^2.2.8"
  },
  "publishConfig": {
    "access": "public"
  }
}
